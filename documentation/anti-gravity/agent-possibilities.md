## Ce que je peux faire pour vous — résumé concis (Markdown)

1. **Assistance au codage**

   * Écriture & modification de code
     *Ex.* créer une fonction `calculateTotal(price, tax)` en JavaScript.
   * Refactorisation
     *Ex.* extraire du code dupliqué dans une fonction utilitaire.
   * Débogage
     *Ex.* comprendre pourquoi une boucle `for` ne s’exécute pas comme prévu.
   * Ajout de tests unitaires
     *Ex.* écrire des tests Jest pour une API REST.

2. **Gestion de projets**

   * Création de fichiers & dossiers (selon la structure du projet)
   * Mise à jour de documentation (README, guides d’installation, etc.)
   * Génération d’artifacts (plans d’implémentation, listes de tâches, walkthroughs)

3. **Recherche & documentation**

   * Recherche de références (API, bibliothèques, bonnes pratiques)
   * Synthèse de documentation
     *Ex.* résumer les options de configuration d’un serveur Express.

4. **Automatisation & exécution de commandes**

   * Proposition de commandes (`npm`, `git`, `Docker`, etc.)
   * Exécution sécurisée (avec confirmation utilisateur)

5. **Création d’images & maquettes**

   * Génération d’illustrations (mockups d’interfaces, icônes) via un générateur d’images

6. **Gestion de workflows**

   * Création & lecture de workflows (fichiers `.md` décrivant des étapes automatisables)
   * Exécution partielle de steps marqués `// turbo` ou `// turbo-all`

7. **Interaction avec le navigateur**

   * Automatisation de navigation (remplir des formulaires, capturer des captures d’écran)

8. **Recherche web & extraction de contenu**

   * Recherche d’informations via le moteur de recherche intégré
   * Lecture de pages web (conversion en Markdown)

9. **Gestion de tâches & suivi**

   * Création d’un fichier `task.md` avec checklist détaillée
   * Mise à jour du statut pour suivre l’avancement

10. **Communication claire**

* Réponses structurées (titres, listes, blocs de code)
* Demande de clarification quand le besoin n’est pas évident

### Exemple de réponse formatée (Markdown)

```js
// Exemple de fonction JavaScript
function calculateTotal(price, taxRate) {
  return price + price * taxRate;
}

// Utilisation
calculateTotal(100, 0.2); // → 120
```

```js
// Test Jest
test('calculates total with tax', () => {
  expect(calculateTotal(50, 0.1)).toBe(55);
});
```

> **Note** : Toutes les actions sont réalisées en respectant vos contraintes de chemin (ex. : `\\wsl.localhost\Ubuntu\home\loop\projects\car-rental\CarRentalUI`).
> Dites-moi simplement la tâche et le format souhaité, et je m’adapte.
